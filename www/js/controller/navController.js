// The scope allows to pass variables to the view
function navController($scope){
	// Default case, the menu is active to "library"
	$scope.menu = 'library';
	// And on the HTML, when we clic on each nav link, the active class and the variable change
}